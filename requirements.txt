# Manuel Kumas Kuratorluk Paneli (v3.0) — bulut (Supabase + Render)
flask>=3.0
Pillow>=11.0
supabase>=2.6
python-dotenv>=1.0
gunicorn>=21.2

# v4.0-part-2 Adim 7 — Notlar sekmesi (rich-text HTML sanitize)
bleach>=6.0
tinycss2>=1.2

# Opsiyonel: iPhone HEIC yuklemesi gerekirse
# pillow-heif>=0.18
