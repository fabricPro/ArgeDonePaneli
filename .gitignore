# Yuklenen/indirilen gorseller — buyuk binary, git'e girmez (panel diske yazar)
gorseller/
pdfler/
02_gorseller/

# urunler/ COMMIT EDILIR — kucuk lean JSON kayitlari (sistem-of-record)

# Ham veri / model artifacts (nerede olursa olsun ignore — _arsiv dahil)
ham_cikti/
logs/
*.pkl
*.npy

# Gizli anahtarlar — ASLA commit etme
.env

# Python
*.pyc
__pycache__/
.venv/

# Misc
.DS_Store
*.bak

# v3.9 numune-react — eski React Vite deneme, artık kullanılmıyor (vanilla JS port edildi)
numune-react/

# Ust git repo'nun worktree mekanizmasi
.claude/
